# ── FRI_collect 依赖清单 ────────────────────────────────────────
# 推荐方式: pip install FRInterp
# 或开发模式: pip install -e .[dev]
#
# 此文件供 pip install -r requirements.txt 使用
# 生产环境建议直接用 pyproject.toml（声明式）

# === 核心依赖（必需）===
numpy>=1.21
scipy>=1.7
h5py>=3.0
pandas>=1.3
eccodes>=2.30        # ECMWF GRIB2 编解码（C库 + Python 绑定）
rasterio>=1.3         # GeoTIFF 地形数据读取

# === 可视化（可选）===
# matplotlib>=3.5
# cartopy>=0.21
# seaborn>=0.12

# === 开发工具（可选）===
# pytest>=7.0
# pytest-cov>=4.0
# ruff>=0.1
